apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ocproutingservice.fullname" . }}-test-connection"
  labels:
    {{- include "ocproutingservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ocproutingservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
