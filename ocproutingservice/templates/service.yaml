apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocproutingservice.fullname" . }}
  labels:
    {{- include "ocproutingservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocproutingservice.selectorLabels" . | nindent 4 }}
