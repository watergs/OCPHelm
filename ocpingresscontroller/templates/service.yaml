apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocpingresscontroller.fullname" . }}
  labels:
    {{- include "ocpingresscontroller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ocpingresscontroller.selectorLabels" . | nindent 4 }}
