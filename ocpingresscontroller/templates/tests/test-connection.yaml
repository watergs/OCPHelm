apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ocpingresscontroller.fullname" . }}-test-connection"
  labels:
    {{- include "ocpingresscontroller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ocpingresscontroller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
